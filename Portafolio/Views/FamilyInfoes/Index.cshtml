@model IEnumerable<Portafolio.Models.FamilyInfo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CriminalRecord)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Residence)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Work)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Photo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CriminalRecord)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Residence)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Work)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Photo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FamilyInfoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FamilyInfoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FamilyInfoId })
        </td>
    </tr>
}

</table>
